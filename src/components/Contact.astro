---

---

<section id="contacto" class="section contact">
    <div class="container">
        <div class="contact-wrapper">
            <div class="contact-info">
                <h2 class="section-title">Hablemos de tu proyecto</h2>
                <p>
                    ¿Listo para escalar tu negocio? Cuéntanos tus desafíos y te
                    ayudaremos a encontrar la solución tecnológica adecuada.
                </p>
                <div class="info-item">
                    <strong>Email:</strong> yeifran67@gmail.com
                </div>

                <a
                    href="https://wa.me/573058079573"
                    target="_blank"
                    class="btn btn-whatsapp"
                >
                    <span class="icon">📱</span> Contactar por WhatsApp
                </a>
            </div>

            <form class="contact-form" id="contact-form">
                <div class="form-group">
                    <label for="name">Nombre</label>
                    <input
                        type="text"
                        id="name"
                        name="user_name"
                        required
                        placeholder="Tu nombre"
                    />
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input
                        type="email"
                        id="email"
                        name="user_email"
                        required
                        placeholder="yeifran67@gmail.com"
                    />
                </div>
                <div class="form-group">
                    <label for="message">Mensaje</label>
                    <textarea
                        id="message"
                        name="message"
                        rows="4"
                        required
                        placeholder="¿En qué podemos ayudarte?"></textarea>
                </div>
                <button type="submit" class="btn btn-primary" id="submit-btn"
                    >Enviar Mensaje</button
                >
                <p id="form-status" class="form-status"></p>
            </form>
        </div>
    </div>
</section>

<script>
    import emailjs from "@emailjs/browser";

    const form = document.getElementById("contact-form") as HTMLFormElement;
    const statusMsg = document.getElementById("form-status");
    const submitBtn = document.getElementById(
        "submit-btn",
    ) as HTMLButtonElement;

    if (form) {
        form.addEventListener("submit", function (e) {
            e.preventDefault();

            if (submitBtn) submitBtn.disabled = true;
            if (statusMsg) statusMsg.textContent = "Enviando...";

            // REPLACE THESE WITH YOUR ACTUAL EMAILJS CREDENTIALS
            const SERVICE_ID = "neuraljira";
            const TEMPLATE_ID = "template_axw8cco";
            const PUBLIC_KEY = "RI4svDyx_om5FK-bF";

            emailjs
                .sendForm(SERVICE_ID, TEMPLATE_ID, this, PUBLIC_KEY)
                .then(
                    () => {
                        if (statusMsg) {
                            statusMsg.textContent =
                                "¡Mensaje enviado con éxito!";
                            statusMsg.style.color = "#4ade80"; // Green
                        }
                        form.reset();
                    },
                    (error) => {
                        console.error("FAILED...", error);
                        if (statusMsg) {
                            statusMsg.textContent =
                                "Error al enviar. Por favor intenta de nuevo.";
                            statusMsg.style.color = "#f87171"; // Red
                        }
                    },
                )
                .finally(() => {
                    if (submitBtn) submitBtn.disabled = false;
                });
        });
    }
</script>

<style>
    .contact {
        background: linear-gradient(
            to bottom,
            var(--color-bg),
            var(--color-bg-secondary)
        );
    }

    .contact-wrapper {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--spacing-xl);
        align-items: start;
    }

    @media (max-width: 768px) {
        .contact-wrapper {
            grid-template-columns: 1fr;
        }
    }

    .contact-info h2 {
        margin-bottom: var(--spacing-md);
    }

    .contact-info p {
        margin-bottom: var(--spacing-lg);
        font-size: 1.1rem;
    }

    .info-item {
        margin-bottom: var(--spacing-lg);
    }

    .contact-form {
        background: var(--color-bg);
        padding: var(--spacing-lg);
        border-radius: var(--radius-lg);
        border: 1px solid var(--border-color);
    }

    .form-group {
        margin-bottom: var(--spacing-md);
    }

    .form-group label {
        display: block;
        margin-bottom: var(--spacing-sm);
        font-weight: 500;
        color: var(--color-text);
    }

    .form-group input,
    .form-group textarea {
        width: 100%;
        padding: 0.75rem;
        background: var(--color-bg-secondary);
        border: 1px solid var(--border-color);
        border-radius: var(--radius-sm);
        color: var(--color-text);
        font-family: inherit;
        transition: border-color 0.2s;
    }

    .form-group input:focus,
    .form-group textarea:focus {
        outline: none;
        border-color: var(--color-primary);
    }

    .btn {
        width: 100%;
    }

    .btn-whatsapp {
        background-color: #25d366;
        color: white;
        margin-top: var(--spacing-md);
        text-decoration: none;
        font-weight: bold;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        width: fit-content;
    }

    .btn-whatsapp:hover {
        background-color: #128c7e;
        transform: translateY(-1px);
    }

    .form-status {
        margin-top: var(--spacing-md);
        font-size: 0.9rem;
        text-align: center;
        min-height: 1.5em;
    }
</style>
