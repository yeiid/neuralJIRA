---

---

<section id="inicio" class="hero">
    <div class="container hero-content">
        <h1 class="title">
            Impulsa tu Startup con <br />
            <span class="text-gradient">Tecnología a Medida</span>
        </h1>
        <p class="subtitle">
            Ofrecemos asesoría experta y desarrollo de software personalizado
            para ayudarte a escalar tu negocio de manera eficiente y robusta.
        </p>
        <div class="cta-group">
            <a href="#contacto" class="btn btn-primary">Empezar ahora</a>
            <a href="#servicios" class="btn btn-secondary">Ver servicios</a>
        </div>
    </div>

    <div class="hero-bg"></div>
</section>

<style>
    .hero {
        position: relative;
        padding: 180px 0 100px;
        overflow: hidden;
        text-align: center;
    }

    .hero-content {
        position: relative;
        z-index: 1;
        max-width: 800px;
    }

    .title {
        margin-bottom: var(--spacing-lg);
    }

    .subtitle {
        font-size: 1.25rem;
        margin-bottom: var(--spacing-xl);
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    .cta-group {
        display: flex;
        gap: var(--spacing-md);
        justify-content: center;
    }

    .hero-bg {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 100%;
        height: 100%;
        background: radial-gradient(
            circle at center,
            rgba(99, 102, 241, 0.15) 0%,
            transparent 70%
        );
        z-index: 0;
        pointer-events: none;
    }
</style>
