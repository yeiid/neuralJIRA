---
const projects = [
    {
        title: "Restaurante",
        description:
            "Sistema integral de pedidos y gestión de mesas para alta gastronomía.",
        tags: ["App Móvil", "Dashboard", "Real-time"],
        icon: "🍽️",
    },
    {
        title: "GymTrack",
        description:
            "Plataforma de seguimiento de rutinas y gestión de membresías.",
        tags: ["SaaS", "Analytics", "Pagos"],
        icon: "💪",
    },
    {
        title: "Tienda Online",
        description:
            "E-commerce escalable con gestión de inventario automatizada.",
        tags: ["E-commerce", "Stripe", "SEO"],
        icon: "🛍️",
    },
    {
        title: "Administración",
        description:
            "ERP personalizado para optimización de recursos empresariales.",
        tags: ["B2B", "Cloud", "Seguridad"],
        icon: "📊",
    },
];
---

<section id="proyectos" class="section projects">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Proyectos Destacados</h2>
            <p class="section-subtitle">
                Casos de éxito que demuestran nuestra capacidad
            </p>
        </div>

        <div class="projects-grid">
            {
                projects.map((project) => (
                    <div class="project-card">
                        <div class="card-content">
                            <div class="project-icon">{project.icon}</div>
                            <h3 class="project-title">{project.title}</h3>
                            <p class="project-description">
                                {project.description}
                            </p>
                            <div class="tags">
                                {project.tags.map((tag) => (
                                    <span class="tag">{tag}</span>
                                ))}
                            </div>
                        </div>
                        <div class="card-overlay" />
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .projects {
        background-color: var(--color-bg);
    }

    .section-header {
        text-align: center;
        margin-bottom: var(--spacing-xl);
    }

    .projects-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: var(--spacing-lg);
    }

    .project-card {
        position: relative;
        background: var(--color-bg-secondary);
        border-radius: var(--radius-lg);
        overflow: hidden;
        border: 1px solid var(--border-color);
        transition:
            transform 0.3s,
            box-shadow 0.3s;
        height: 100%;
    }

    .project-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.5);
        border-color: var(--color-primary);
    }

    .card-content {
        padding: var(--spacing-lg);
        position: relative;
        z-index: 2;
        height: 100%;
        display: flex;
        flex-direction: column;
    }

    .project-icon {
        font-size: 3rem;
        margin-bottom: var(--spacing-md);
        background: var(--gradient-main);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        width: fit-content;
    }

    .project-title {
        font-size: 1.5rem;
        margin-bottom: var(--spacing-sm);
        color: var(--color-text);
    }

    .project-description {
        color: var(--color-text-muted);
        margin-bottom: var(--spacing-lg);
        flex-grow: 1;
    }

    .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }

    .tag {
        font-size: 0.75rem;
        padding: 0.25rem 0.75rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 999px;
        color: var(--color-text-muted);
        border: 1px solid var(--border-color);
    }

    .card-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(
            circle at top right,
            rgba(99, 102, 241, 0.1),
            transparent 60%
        );
        opacity: 0;
        transition: opacity 0.3s;
        pointer-events: none;
        z-index: 1;
    }

    .project-card:hover .card-overlay {
        opacity: 1;
    }
</style>
